[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "./scripts/start_backend.sh"
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
